# split-brain

Goal: run SFT with KL regularization via `src/jaxformers/train/ntp_with_kl_regularzier.py`.

Status: config scaffolded; no runs recorded yet.

Best command:

```bash
uv run python src/jaxformers/train/ntp_with_kl_regularzier.py --config experiments/split-brain/base_config.py
```

Current best result: no runs yet.
